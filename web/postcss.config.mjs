/** @type {import('postcss-load-config').Config} */
// Tailwind CSS v4 is handled via @tailwindcss/vite in astro.config — no PostCSS plugin needed.
const config = { plugins: {} }
export default config
